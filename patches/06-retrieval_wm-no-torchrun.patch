diff --git a/hyworld2/worldgen/src/retrieval_wm.py b/hyworld2/worldgen/src/retrieval_wm.py
index 401a039..66f7f43 100755
--- a/hyworld2/worldgen/src/retrieval_wm.py
+++ b/hyworld2/worldgen/src/retrieval_wm.py
@@ -3,6 +3,7 @@ import json
 import math
 import os
 import subprocess
+import sys
 from concurrent.futures import ThreadPoolExecutor
 from glob import glob
 from typing import Tuple, List
@@ -1132,7 +1133,7 @@ class PanoramaMemoryBank:
         if self.rank == 0:
             if not (skip_exist and os.path.exists(f"{self.world_mirror_dir}/name_map.json")):
                 wm_cmd = [
-                    "torchrun", f"--nproc_per_node={self.world_size}", "-m", "worldrecon.pipeline",
+                    sys.executable, "-m", "worldrecon.pipeline",
                     "--input_path", f"{self.world_mirror_dir}/images",
                     "--prior_cam_path", f"{self.world_mirror_dir}/cameras.json",
                     "--strict_output_path", f"{self.world_mirror_dir}/results",
@@ -1144,12 +1145,15 @@ class PanoramaMemoryBank:
                     "--no_save_points",
                     "--no_sky_mask",
                     "--no_edge_mask",
-                    "--use_fsdp",
                     "--enable_bf16",
                     "--disable_heads", "normal", "points", "gs"
                 ]
-                color_print(f"[Rank0] Running World Mirror inference: {' '.join(wm_cmd)}", "info")
-                result = subprocess.run(wm_cmd, cwd="..")
+                wm_env = os.environ.copy()
+                wm_env.update({"RANK": "0", "WORLD_SIZE": "1", "LOCAL_RANK": "0",
+                               "MASTER_ADDR": "127.0.0.1", "MASTER_PORT": "29509",
+                               "USE_LIBUV": "0"})
+                color_print(f"[Rank0] Running World Mirror inference (single-GPU): {' '.join(wm_cmd)}", "info")
+                result = subprocess.run(wm_cmd, cwd="..", env=wm_env)
 
                 if result.returncode != 0:
                     raise RuntimeError(f"World Mirror inference failed with return code {result.returncode}")
